# 阶段交付说明_20260531

## 交付内容

2026-05-31：本阶段交付智能货柜管理端本地可运行版本，包括后端管理接口、前端管理页面、支付宝沙箱验证台、演示数据维护和回归测试。

## GitHub 仓库

| 仓库 | 地址 | 分支 |
|---|---|---|
| 后端 | `https://github.com/Tyron-G/smart-container` | `master` |
| 前端 | `https://github.com/Tyron-G/smart-container-admin` | `main` |

## 本地启动

后端：

```powershell
cd D:\smart-container
pwsh -NoProfile -ExecutionPolicy Bypass -File .\work\启动支付宝沙箱环境_20260531.ps1
```

前端：

```powershell
cd D:\smart-container-admin
npm install
npm run dev
```

访问地址：

```text
http://localhost:5173
```

默认本地账号：

| 账号 | 密码 | 说明 |
|---|---|---|
| `admin` | `admin123` | 超级管理员 |
| `operator` | `operator123` | 运营人员，用于权限回归 |

## 关键路径

| 功能 | 路由 |
|---|---|
| 工作台 | `/dashboard` |
| 订单管理 | `/order/list` |
| 用户管理 | `/user/list` |
| 设备概览 | `/device/overview` |
| 设备监控 | `/device/monitor` |
| 优惠券流水 | `/coupon/flow` |
| 运营巡检 | `/operations/checkup` |
| 支付验证台 | `/operations/payment-lab` |
| 审计日志 | `/system/audit` |

## 支付配置说明

支付宝沙箱配置通过本地忽略目录 `work/.payment-secrets/alipay-sandbox/` 读取，禁止提交真实密钥、证书和二维码文件。

需要的配置文件：

| 文件 | 说明 |
|---|---|
| `app_id.txt` | 支付宝沙箱 AppId |
| `merchant_private_key_single_line.txt` | 应用私钥单行格式 |
| `alipay_public_key.txt` | 支付宝公钥 |

## 交付边界

- 仅推送 GitHub，不部署服务器。
- 当前支付宝为沙箱验证，不代表生产真实资金已上线。
- 微信支付保持本地联调模式，等待商户资料后再接生产。
- 交付内容不包含任何密钥、证书、二维码、构建产物或本地日志。

## 推荐后续

1. 准备生产服务器或云环境。
2. 配置生产 MySQL、Nacos、网关域名和 HTTPS。
3. 获取支付宝/微信正式商户资料。
4. 将当前管理端功能作为后台基线版本，再推进小程序端和真实设备接入。

