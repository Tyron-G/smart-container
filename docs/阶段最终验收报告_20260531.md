# 阶段最终验收报告_20260531

## 验收范围

2026-05-31：本次验收范围为智能货柜管理端本地联调阶段，覆盖 `smart-container` 后端 `container-manager` 与 `smart-container-admin` 前端管理端。

## 代码版本

| 仓库 | 分支 | 验收提交 |
|---|---|---|
| `smart-container` | `master` | `ec0e090 fix: sanitize alipay refund gateway errors` |
| `smart-container-admin` | `main` | `cc2a588 fix: shorten payment lab refund errors` |

## 功能验收结果

| 模块 | 验收结果 | 说明 |
|---|---|---|
| 登录与权限 | 通过 | 登录、当前用户、页面级权限、按钮级权限和旧 token 修复已联调 |
| 工作台 | 通过 | 汇总优惠券、用户、订单、设备和权限数据 |
| 用户管理 | 通过 | 支持新增、编辑、状态变更、详情、签约记录和订单历史 |
| 订单管理 | 通过 | 支持列表、详情、商品明细、退款、补扣、异常处理和状态修正 |
| 设备管理 | 通过 | 支持设备新增编辑、仓门维护、事件处理、远程开门和重启联调 |
| 角色权限 | 通过 | 支持角色新增编辑停用、权限新增编辑停用和权限树维护 |
| 优惠券 | 通过 | 支持配置新增编辑停用、发放记录、领取记录、使用记录和手工发放 |
| 审计日志 | 通过 | 统一读取 `operation_log`，覆盖业务操作与支付验证台操作 |
| 运营巡检 | 通过 | 汇总支付配置、审计统计、验收进度、关键入口和演示数据维护 |
| 支付验证台 | 通过 | 支付宝沙箱预下单、二维码、交易查询、退款验证和验证记录已联调 |
| 演示数据维护 | 通过 | 支持覆盖度查询和幂等补齐，不清空已有支付、退款、审计记录 |

## 支付验收结论

- 支付宝沙箱预下单、扫码支付、交易查询链路已跑通。
- 支付宝沙箱退款接口已接入真实 SDK 调用，沙箱网关偶发 504 时已做错误清洗，不再展示 HTML 原文。
- 微信支付因缺少真实微信商户号、APIv3 Key、证书序列号和商户私钥，当前保持本地联调模式。
- 生产真实资金链路未默认启用，不会误触发生产扣款或退款。

## 验证命令

后端构建：

```powershell
$env:JAVA_HOME='C:\Program Files\Java\jdk-1.8'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn -pl container-manager -am -DskipTests package
```

前端构建：

```powershell
npm run build
```

浏览器回归：

```powershell
npm run test:e2e
```

最近一次验证结果：

| 项目 | 结果 |
|---|---|
| 后端 Maven 构建 | 通过 |
| 前端 Vite 构建 | 通过 |
| Playwright 回归 | `9 passed` |
| 演示数据接口联调 | `/api/manager/demo/ensure` 后 `ready=true` |

## 已知边界

- 当前为本地开发/演示阶段，未做生产服务器部署。
- 支付宝生产环境仍需要生产商户应用、生产网关、密钥和上线授权。
- 微信真实支付仍需要微信支付商户资料与证书。
- 真实设备协议、物联网平台和小程序真实用户端流程不在本阶段范围内。

