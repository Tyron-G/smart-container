# 部署验证报告

## 验证时间

2026-05-31，北京时间。

## 验证环境

| 项目 | 值 |
| --- | --- |
| 后端项目 | `D:\smart-container` |
| 前端项目 | `D:\smart-container-admin` |
| 后端服务 | `container-manager` |
| 后端端口 | `9965` |
| 前端端口 | `5173` |
| 数据库 | `smart_container` |
| 微信支付 | 本地联调模式 |
| 支付宝 | 沙箱已验证，当前本地服务未启用支付宝环境 |

## 已执行验证

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 后端构建 | `mvn -pl container-manager -am -DskipTests package` | 通过 |
| 后端接口回归 | `pwsh -File .\work\管理端E2E回归测试_20260531.ps1` | 通过 |
| 部署前完整验证 | `pwsh -File .\work\部署前完整验证_20260531.ps1 -SkipBuild` | 通过 |
| 支付配置自检 | `pwsh -File .\work\真实支付通道配置检查_20260531.ps1` | 通过 |
| 微信本地退款 | `pwsh -File .\work\微信本地退款验证_20260531.ps1` | 通过 |
| 前端构建 | `npm run build` | 通过 |
| 页面级回归 | `npm run test:e2e` | 7 条通过 |

## 关键验证结果

- 后端 JAR 已生成：`container-manager/target/container-manager-1.0-SNAPSHOT.jar`。
- 前端 dist 已生成：`D:\smart-container-admin\dist\index.html`。
- 管理端接口回归覆盖订单、退款、补扣、用户、设备、角色权限、优惠券、审计日志和异常场景。
- 页面级 Playwright 回归覆盖登录、权限拦截、旧 token 修复、订单处理、用户表单、设备维护、角色权限、优惠券发放和审计查询。
- 微信本地联调退款记录已写入 `payment_operation_record` 和 `order_refund_record`，请求/响应审计字段均有值。
- 支付宝沙箱已完成支付成功、交易查询和退款成功闭环。

## 生产上线注意事项

- 不得提交或打包 `work/.payment-secrets/`。
- 真实支付密钥应由环境变量、配置中心或密钥管理系统注入。
- 微信真实资金通道上线前必须关闭 `MANAGER_WECHAT_LOCAL_TEST_MODE`。
- 正式环境启用支付前必须先执行 `真实支付通道配置检查_20260531.ps1`。
- 上线前应在目标环境重新执行 `部署前完整验证_20260531.ps1`。
