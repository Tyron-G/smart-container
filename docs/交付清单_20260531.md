# 管理端交付清单

## 交付范围

2026-05-31：智能货柜管理端当前交付范围包括后端 `container-manager`、前端 `smart-container-admin`、数据库初始化 SQL、支付通道联调脚本、回归验证脚本和配套文档。

## 交付物

| 类型 | 内容 | 位置 |
| --- | --- | --- |
| 后端 JAR | `container-manager-1.0-SNAPSHOT.jar` | `container-manager/target/` |
| 前端构建产物 | Vite `dist` | `D:\smart-container-admin\dist` |
| 数据库脚本 | 建表、测试数据、管理端功能补齐、权限初始化、支付审计字段 | `work/*.sql` |
| 验证脚本 | 管理端 E2E、部署前完整验证、支付配置检查 | `work/*.ps1` |
| 支付脚本 | 支付宝沙箱、微信本地联调、真实通道配置模板 | `work/*支付*.ps1` |
| 文档 | 接口、数据库、支付、部署验证说明 | `docs/` |

## 交付包生成

在后端项目根目录执行：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\work\生成交付包_20260531.ps1
```

输出目录：

```text
work/交付包_20260531/
```

交付包不包含 `work/.payment-secrets/`、日志文件、`node_modules`、`target` 目录和任何真实密钥。

## 当前功能状态

- 登录、当前用户、页面级权限、按钮级权限已接入后端。
- 订单管理已支持列表、详情、商品明细、退款、补扣、异常处理和状态修正。
- 用户管理已支持新增、编辑、详情、签约记录、订单历史和状态变更。
- 设备管理已支持设备新增/编辑、仓门维护、事件处理、远程开门和重启事件。
- 角色权限已支持角色新增/编辑/停用、权限新增/编辑/停用和角色授权。
- 优惠券已支持配置管理、发放记录、领取记录、使用记录、手工发放和撤回。
- 审计日志已接入管理端操作日志。
- 支付宝沙箱已完成支付、查询、退款闭环。
- 微信支付开发阶段采用本地联调模式，生产真实资金需微信商户资料。

## 上线前限制

- 生产真实微信支付需要微信支付商户号、APIv3 Key、商户证书序列号和 `apiclient_key.pem`。
- 生产真实支付宝支付需要正式环境 APPID、应用私钥和支付宝公钥。
- 默认配置不会触发真实资金通道，必须显式启用对应环境变量或配置中心参数。
