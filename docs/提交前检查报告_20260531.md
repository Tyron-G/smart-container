# 提交前检查报告

## 检查时间

2026-05-31，北京时间。

## 检查范围

- 后端仓库：`D:\smart-container`
- 前端仓库：`D:\smart-container-admin`
- 管理端后端服务：`container-manager`
- 管理端前端：`smart-container-admin`

## 忽略规则

2026-05-31：已确认以下本地产物和敏感目录不会进入源码提交：

| 仓库 | 路径 | 状态 |
| --- | --- | --- |
| 后端 | `work/.payment-secrets/` | 已忽略 |
| 后端 | `work/交付包_*/` | 已忽略 |
| 后端 | `work/*交付包*.zip` | 已忽略 |
| 前端 | `dist/` | 已忽略 |
| 前端 | `node_modules/` | 已忽略 |
| 前端 | `test-results/` | 已忽略 |

## 协作文档一致性

后端根目录 `CLAUDE.md`、`AGENTS.md`、`GEMINI.md` 哈希一致，满足项目协作说明的 SSOT 要求。

## 敏感信息扫描

2026-05-31：对后端和前端待提交/已修改文件执行敏感模式扫描，未发现以下高风险内容：

- `BEGIN PRIVATE KEY`
- `apiclient_key.pem`
- `apiclient_cert.pem`
- 微信 `APIv3 Key` 明文模式
- 支付宝商户私钥明文模式
- 支付宝公钥长串配置模式

扫描排除了已忽略目录、构建产物、日志和交付压缩包。

## 已完成验证

| 验证项 | 结果 |
| --- | --- |
| 后端 `container-manager` 构建 | 通过 |
| 后端管理端 E2E 回归 | 通过 |
| 部署前完整验证 | 通过 |
| 前端构建 | 通过 |
| 前端 Playwright 页面级回归 | 7 条通过 |
| 交付包生成 | 通过 |
| 交付包敏感内容检查 | 未发现密钥、证书、日志、依赖目录 |

## 提交建议

- 提交源码、SQL、脚本和文档。
- 不提交 `work/.payment-secrets/`、交付包目录、交付 zip、日志、前端 `dist/`、`test-results/` 和 `node_modules/`。
- 真实支付密钥继续通过环境变量、配置中心或本地忽略目录注入。
